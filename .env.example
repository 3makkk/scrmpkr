# ==== Traefik / Networking ====
TRAEFIK_NETWORK=traefik_proxy
TRAEFIK_ENTRYPOINTS=websecure
# set to 'true' if your Traefik requires TLS on the chosen entrypoints
TRAEFIK_TLS=true

# Optional image registry/tag for Swarm
REGISTRY=scrmpkr
IMAGE_TAG=latest

# ==== Public Hosts ====
# e.g. app.example.com and api.example.com
FRONTEND_HOST=scrmpkr.friedemann.dev
SERVER_HOST=scrmpkr.friedemann.dev
API_PATH_PREFIX=/api

# ==== Frontend Build Vars (public) ====
# Public URL for API (scheme + host + prefix)
VITE_API_URL=https://scrmpkr.friedemann.dev/api

# ==== Server Runtime Vars (secrets: set only in real .env) ====
# CORS must match the deployed frontend origin (no path)
CORS_ORIGIN=https://scrmpkr.friedemann.dev
